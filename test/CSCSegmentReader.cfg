process TESTing = { 
        untracked PSet maxEvents = {untracked int32 input = -1}
  	source = PoolSource { 
		untracked vstring fileNames = {
"file:/tmp/dfortin/cscsegments_shower.root"
#"file:/tmp/dfortin/cscsegments_shower_no_prune.root"
#"file:/tmp/dfortin/A24B9D16-36C2-DC11-B292-000423D9870C.root"
} 
  	}

    # es_source of ideal geometry
    # ===========================
    # endcap muon only...
    include "Geometry/MuonCommonData/data/muonEndcapIdealGeometryXML.cfi"
    es_module = MuonNumberingInitialization { }

    # flags for modelling of CSC geometry
    # ===================================
    include "Geometry/CSCGeometry/data/cscGeometry.cfi"

  	module cscSegmentReader = CSCSegmentReader {
        untracked string RootFileName = "cscsegments_shower_zoom.root"
        untracked int32 minLayerWithRechitPerChamber = 6
        untracked int32 minLayerWithSimhitPerChamber = 6
	untracked int32 maxNhits                     = 999
	untracked int32 minNhits                     = 20
        untracked int32 minRechitPerSegment = 5
        untracked double maxThetaSeparation = 0.2
        untracked double maxPhiSeparation   = 0.5
	}

  	path p = {cscSegmentReader}
}
