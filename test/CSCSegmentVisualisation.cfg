process TESTing = { 
        untracked PSet maxEvents = {untracked int32 input = -1}
  	source = PoolSource { 
		untracked vstring fileNames = {"file:cscsegment.root"} 
  	}

    # es_source of ideal geometry
    # ===========================
    # endcap muon only...
    include "Geometry/MuonCommonData/data/muonEndcapIdealGeometryXML.cfi"
    es_module = MuonNumberingInitialization { }

    # flags for modelling of CSC geometry
    # ===================================
    include "Geometry/CSCGeometry/data/cscGeometry.cfi"

  	module cscSegmentVis = CSCSegmentVisualise{
        untracked string RootFileName = "cscsegments_plot.root"
        untracked int32 minRechitPerChamber = 10
	untracked int32 maxRechitPerChamber = 25
	}

  	path p = { cscSegmentVis }
}
