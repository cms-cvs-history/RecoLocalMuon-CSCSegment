process PROD2 = {
	source = PoolSource {
                 untracked vstring fileNames = {
                      'file:/uscmst1b_scratch/lpc1/lpcmuon/ibloch/dfortin/run_4241/cscsegments-sk-old.root'
                 }
     		 untracked int32 maxEvents = 20000
    		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = false
		}
 
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/CSCGeometry/data/cscGeometry.cfi"
        #replace CSCGeometryESModule.useCentreTIOffsets = true

        include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

	module cscanalysis = CSCMTCCAnalysis{
		# Switch on/off the verbosity and turn on/off histogram production
		untracked bool debug = false
		# Label to retrieve RecHits from the event
                untracked string recHitLabel = 'csc2DRecHits'
                untracked string cscSegmentLabel = 'cscSegments'
		# Name of the root file which will contain the output histograms
		untracked string rootFileName = 'mtccplots_4241_test1.root'
		#untracked string rootFileName = "csc_mtccplots_282_0_2beta.root"
	        # Maximum dx/dz of segments
	        untracked double maxSlopedxdz = 0.3
	        # Maximum dy/dz of segments
	        untracked double maxSlopedydz = 1.0
		# the left x margin (# of strips away form det. edge) of chambers
	        untracked int32 x_margin_L = 2
		# the right x margin (# of strips away form det. edge) of chambers
	        untracked int32 x_margin_R = 2
	        # the top y margin (cm) of chambers
	        untracked double y_margin_T = 2.0
	        # the bottom y margin (cm) of chambers
	        untracked double y_margin_B = 2.0
               
	}
	
# Useful to debug:
#	module ana = EventContentAnalyzer {untracked int32 debugLevel = 2}
#	path p = {ana,rechitreader}
	path p = {cscanalysis}
}

