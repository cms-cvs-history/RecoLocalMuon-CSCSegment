process PROD2 = {
	source = PoolSource {
                 untracked vstring fileNames = {
                      'file:/uscmst1b_scratch/lpc1/lpcmuon/ibloch/dfortin/run_4241_calib/cscsegments-tc-new2.root'
                 }
     		 untracked int32 maxEvents = -1
    		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = false
		}
 
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/CSCGeometry/data/cscGeometry.cfi"
        include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

	module cscanalysis = CSCMTCCOverlap{
		# Switch on/off the verbosity and turn on/off histogram production
		untracked bool debug = false
		# Label to retrieve RecHits from the event
                untracked string recHitLabel = 'csc2DRecHits'
                untracked string cscSegmentLabel = 'cscSegments'
		# Name of the root file which will contain the output histograms
		untracked string rootFileName = 'overlap_4241_sk.root'
	        # Maximum dx/dz slope for segments
	        untracked double maxSlopedxdz = 0.3
	        # Maximum dy/dz slope for segments
	        untracked double maxSlopedydz = 0.5
	        # Mininum of hits per segment
	        untracked int32 MinNhits = 5
		# Maximum Delta phi distance between origin of segment and extrapolation from other segment
                untracked double maxDphi = 0.349   
                # the above value in rads corresponds to 20 degrees 
		# Maximum diffence in the cosine of the angle between the two segment vectors
                untracked double minCosTheta12 = 0.9
	}
	
	path p = {cscanalysis}
}

